{% extends 'layouts/base.html.twig' %}

{% block title %}Réinitialisez votre mot de passe{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">

            <h1 class ="mt-5 mb-3 text-center">Réinitialisez votre mot de passe</h1>

            {% for flashError in app.flashes('reset_password_error') %}
                <div class="mb-3 alert alert-error rounded" role="alert">{{ flashError }}</div>
            {% endfor %}

            <p class="mb-5 text-center text-muted">
                Entrez votre adresse e-mail et nous vous enverrons un 
                lien pour réinitialiser votre mot de passe.
            </p>

            {{ form_start(requestForm) }}
            {{ form_row(requestForm.email) }}

            <button class="btn btn-primary btn-lg btn-block">Envoyer un e-mail de réinitialisation</button>
            {{ form_end(requestForm) }}
        </div>
    </div>
{% endblock %}